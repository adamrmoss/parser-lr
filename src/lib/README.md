# Library (`src/lib`)

Browser- and Node-safe parser API. Built with `tsc` into `dist/lib/` as unbundled ESM for consumer tree-shaking.

- `index.ts` — public exports
- `parser-lr.ts` — `ParserLr` class
